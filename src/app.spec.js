import React from "react";
import LevelOne from "./components/LevelOne";

test("Check States of Badges", () => {
  // Render a checkbox with label in the document
  <LevelOne LevelOneRed={true} LevelOneBlue={false} LevelOneGreen={true} />;
});
